# Nexe-Fundacio
Design and implementation of an interactive recreational environment aimed at children with multiple disabilities

To install on dependencies first activate de virtual environment:

source .NexeEnv/bin/activate

And next you can install all libraries:

pip install -r requirements.txt

